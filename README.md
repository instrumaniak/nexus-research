# Nexus

Nexus is a self-hosted, multi-user AI research assistant. Users can ask questions, search the web, build a private knowledge base, and run deep research sessions. The platform is designed for a small community (up to a few hundred users) with an approval-gated registration model.

### Stack
NestJS (TypeScript) + React (Vite) + SQLite
